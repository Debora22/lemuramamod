'use strict';

/**
 * @ngdoc object
 * @name op.track
 * @description
 *
 * This is the main script for the module and will contain the
 * enabled endpoints to be consumed.
 */
angular
    .module('op.track', []);
